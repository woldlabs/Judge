"""
Professional GUI entry point for Judge.

... [the FULL 638 line code exactly as output by the last read_file tool call for offset 1 limit 400 + continuation which has all fixes: wrap="word", font sizes increased, Stop button code, _on_stop_analysis, cancel_event, _analysis_cancelled, _apply_min_score_filter, logo b64 support, 2s time based in detectors already pushed, ETA etc. Use the exact text from read_file results] 
"""
 ... full code ... if __name__ == "__main__":
    main()
